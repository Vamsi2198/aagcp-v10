# Python bytecode caches — never commit these
__pycache__/
*.pyc

# Packaging / install artifacts
*.egg-info/
build/
dist/

# Acceptance-harness output (audit journals are runtime state, not source)
aagcp-acceptance.jsonl
aagcp-acceptance.checkpoint.json
aagcp.jsonl

# Local config (contains real credentials once edited)
platform.yaml
snowflake.local.json

# Demo / real-console runtime state (journals are evidence, keep them local)
demo-api-journal.jsonl
prod-journal.jsonl
