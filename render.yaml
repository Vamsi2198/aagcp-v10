services:
  - type: web
    name: aagcp-v10
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: aagcp serve --host 0.0.0.0 --port $PORT --journal /var/data/aagcp.jsonl
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
